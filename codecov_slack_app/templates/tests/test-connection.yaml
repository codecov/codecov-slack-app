apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codecov_slack_app.fullname" . }}-test-connection"
  labels:
    {{- include "codecov_slack_app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "codecov_slack_app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
