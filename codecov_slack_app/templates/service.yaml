apiVersion: v1
kind: Service
metadata:
  name: {{ include "codecov_slack_app.fullname" . }}
  labels:
    {{- include "codecov_slack_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codecov_slack_app.selectorLabels" . | nindent 4 }}
